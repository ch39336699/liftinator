spring:
  application:
    name: edge-service
server:
  port: 8081
  shutdown: graceful
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    metrics:
      enabled: true
    health:
      show-details: always
  security:
    enable: false
